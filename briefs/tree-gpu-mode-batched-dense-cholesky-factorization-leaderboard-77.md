# Exploration Brief — GPU MODE — Batched Dense Cholesky Factorization (leaderboard 776)

> Distilled from a recursive idea tree seeded by your curation: 11 unread saves read, 10 signals scouted, 22 ideas explored (1 pruned). Every idea carries receipts back into your library or to discovered sources.

## 1. One-block-per-matrix fused shared-memory Cholesky for the tiny/small-n high-batch regime · score 9/10


For all entries where n<=256 (batch=4096/n=32, 1024/64, 256/128, 64/256, and the high-batch 640/512), assign exactly one CUDA thread block per matrix and factorize entirely in shared memory using intra-block __syncthreads barriers. Load the lower triangle into shared memory once, run an unblocked right-looking Cholesky (compute diag sqrt, scale column, rank-1 trailing update) with the whole block cooperating, then write back. This eliminates all per-batch library launch overhead — the dominant cost in this regime per Horace He's overhead taxonomy. For n=32 use sub-warp / one-warp-per-matrix packing so multiple matrices share a block and stay register/shared resident, matching MAGMA's 'tiny' special-cased design (up to 11.8x on V100). Tune shared-mem tiling and warp assignment per shape. This regime is ~half the geomean entries, so winning it dominates the score.

**Deep-dive verdict.** This is the highest-leverage idea in the tree for this leaderboard. The benchmark grid is dominated by high-batch small-n entries (n=32 through 256 plus 640/512), and the fresh MAGMA evidence directly validates the mecha Risks: (1) The parent lumps n=256 and 640/512 with n=32; the evidence is explicit that pure whole-matrix-in-shared only works cleanly to ~32x32 — n=128/256/512 need panel/blocking, so this is really two different kernels and the parent under-specifies the crossover. (2) Register vs shared choice is non-obvious and must be swept per size (Volta deprecates implicit warp-sync, so shared needs explicit __syncwarp/__syncthreads). (3) Correctness: unblocked right-looking POTF2 must guarantee positive diagonal and pass the reconstruction residual under FP32 for ill-conditioned (high cond) inputs — numerical care needed. (4) Occupancy: shared-mem footprint directly caps TBs/SM, so tiling is a real optimization problem not a formality.

**Receipts:** [https://icl.utk.edu/files/publications/2016/icl-utk-909-2016.pdf] ("one thread block per matrix with intra-block barrier sync ('big-tile' setting) w"); [https://www.sciencedirect.com/science/article/abs/pii/S1877750316305154] ("special sub-warp-sized 'tiny' matrix designs (n<=32) delivering up to 11.8x spee"); [doc_0800256bddaa9fe09044228b3ba496b3] ("the small-batch small-n Cholesky entries are launch-overhead-bound (favor a sing"); [https://docs.nvidia.com/cuda/cusolverdx/get_started/potrf.html] ("cuSOLVERDx exposes POTRF as an inlineable __device__ function operating on share")

## 2. Register-resident warp-shuffle Cholesky for n<=32 with N matrices packed per block · score 8/10

*Lineage: One-block-per-matrix fused shared-memory Cholesky for the tiny/small-n high-batch regime → this*

For the batch=4096/n=32 entry (and reusable for n=64 sub-tiles), store each matrix in registers — one thread owns one row — and perform the rank-1 trailing update via __shfl_sync broadcasts of the pivot column instead of shared memory, per MAGMA's register-file version. Pack multiple 32x32 matrices per block (one warp per matrix) so 8+ matrices stay resident; registers are faster than shared (256KB register file vs 64KB shmem on P100) and avoid the __syncthreads cost that dominates tiny sizes. Sweep matrices-per-block for occupancy. This is the single largest-batch entry (4096) and geomean-critical.

**Deep-dive verdict.** This is the sharpest known technique for the single most batch-heavy, geomean-critical entry (batch=4096/n=32), and the MAGMA 'tiny matrices <=32' literature is directly on point — register-resident, one-warp-per-matrix, Risks: Register pressure caps matrices-per-block and occupancy; clean one-thread-per-row mapping only holds at exactly n<=32 (n=64 breaks it); P100 register/shmem framing may be stale on modern GPUs; correctness of shfl-based triangular indexing is fiddly; absolute time saved on the tiniest matrices may be small relative to geomean weight.

**Receipts:** [https://par.nsf.gov/servlets/purl/10065617] ("If threads read into registers, each thread has direct access to an entire row o"); [https://icl.utk.edu/files/publications/2018/icl-utk-1056-2018.pdf] ("target sizes are very small (less than 32 x 32), it is beneficial to keep the wh"); [https://www.sciencedirect.com/science/article/abs/pii/S1877750316305154] ("a different mindset and techniques are needed when matrices are tiny, and in par")

## 3. Fuse interleaved gather into the warp-shuffle kernel's load phase (zero-restage) · score 7/10

*Lineage: One-block-per-matrix fused shared-memory Cholesky for the tiny/small-n high-batch regime → Register-resident warp-shuffle Cholesky for n<=32 with N matrices packed per block → Interleaved (transposed-batch) memory layout feeding the register-resident n=32 kernel → this*

Do NOT materialize a transposed tensor. Instead, in the register-resident n=32 kernel, compute the interleaved global address at load time so lane t of warp w reads element (row_t, col) of matrix m at address base + (row*n+col)*batch + m — i.e. the strided/interleaved index — turning each warp's load of a given (row,col) across its resident matrices into a single coalesced 128B transaction. This gets the coalescing win of interleaving without the separate permute pass the parent worries about. A/B directly against canonical-layout loads in the same kernel; keep whichever wins on the 4096/n=32 entry.

**Receipts:** [https://icl.utk.edu/files/publications/2017/icl-utk-1364-2017.pdf] ("the interleaved layout is a permutation of the strided memory layout"); [https://moonmath.ai/cdna3attention/] ("the transpose kernel writes V in exactly the order the PV matmul consumes, so ea")

## 4. Right-looking blocked GEMM-heavy factorization for the single large matrix (n>=4096) entries · score 6/10


For n=4096, 8192, 16384, 32768 (batch 1-2), the problem is compute-bound, so lean on a classic right-looking blocked Cholesky: partition into NBxNB tiles, factor the diagonal panel (POTRF), TRSM the panel below, then SYRK/GEMM the trailing submatrix — the trailing update is the FLOP sink and should route to cuBLAS SGEMM (near-peak TF32/FP32) or a custom pipelined CUTLASS GEMM. Key win over cuSOLVER-as-is: a custom batched DSYRK that writes only the lower triangle (Dongarra reports ~4x over cuBLAS-GEMM-based syrk) and software pipelining (overlap tile loads with compute) per the Colfax GEMM tutorial. Sweep block size NB (128-512) against the roofline. Since these single-matrix entries carry equal geomean weight to a tiny-matrix entry, a well-tuned blocked path is essential but individually lower-leverage than the batched regime.

**Deep-dive verdict.** This is a necessary but individually lower-leverage path, and the target's own framing admits it. The single-matrix entries (n=4096..32768, batch 1-2) carry equal geomean weight but represent only ~5 of 15 entries, and t Risks: Main risk: reinventing what cuSOLVER already does near-peak, spending hours for a few-percent gain on 2-3 entries. Secondary: TF32 trailing updates risk failing the FP32 reconstruction-residual checker for high-cond inputs — must verify accuracy tolerance before trusting TF32 SGEMM. Duplication risk with the cuSOLVER-fallback frontier sibling. The custom-SYRK win is misattributed from the batched-small regime.

**Receipts:** [https://www.netlib.org/utk/people/JackDongarra/PAPERS/fast-batch-2014.pdf] ("right-looking SYRK trailing update, with a custom batched dsyrk that writes only"); [doc_5f11c747ee974d16234fa90c6b91cb9c] ("Software pipelining to overlap memory copy with compute is directly applicable t"); [doc_0800256bddaa9fe09044228b3ba496b3] ("the 32768x32768 entry is compute-bound (favor blocked GEMM-heavy right-looking a")

## 5. Per-shape kernel dispatch driven by an offline roofline classification · score 6/10


Rather than one algorithm, build a dispatcher that classifies each of the 15 benchmark shapes into a regime (launch-overhead-bound tiny-batch, occupancy-bound mid, bandwidth/compute-bound large) using a roofline model, and picks the pre-tuned kernel + config for each. Concretely: precompute a static lookup keyed on (batch, n) mapping to {fused-one-block, warp-packed-tiny, blocked-right-looking, cuSOLVER-fallback} plus tile sizes. This is exactly MAGMA's finding that 'classic' vs 'big-tile' settings win for different sub-tasks. Because ranking is geomean across all entries, no single entry can be neglected — a dispatcher that avoids the worst-case-per-shape is the geomean-optimal meta-strategy. Low-risk, high-leverage scaffolding that makes the other ideas composable.

**Deep-dive verdict.** The dispatcher is genuinely the geomean-optimal meta-strategy for this leaderboard: ranking is geomean across 15 heterogeneous shapes, so the winning submission must not blow any single entry, and a per-(batch,n) static  Risks: Over-engineering: a runtime roofline classifier is unnecessary when all 15 shapes are known offline — a literal if/elif table suffices. Zero standalone speedup; value is parasitic on sibling kernels. Autotuning/measurement dispatch (perf_dispatch, MAB) adds warmup sync overhead that could hurt on the single-shot large-n entries if not cached correctly. Risk of premature scaffolding before kernels exist to route between.

**Receipts:** [https://icl.utk.edu/files/publications/2016/icl-utk-909-2016.pdf] ("the geomean-optimal strategy dispatches different kernel configs per benchmark s"); [doc_ba165183b5f6f46b622bc9311ed2eee6] ("Roofline-style visualization helps decide per-benchmark-shape which regime ... e"); [https://www.sciencedirect.com/science/article/abs/pii/S1877750316305154] ("A single unified device-routine code base serves both the small-batch (batch) an")

## 6. cuSOLVER batched potrfBatched / cuBLAS baseline as correctness floor and fallback · score 6/10


Ship torch.linalg.cholesky or cusolverDnSpotrfBatched as a first-cut baseline that is guaranteed correct against the property-based checker (lower-triangular, positive diagonal, low residual), then only replace shapes where a custom kernel measurably beats it. This is the pragmatic 'ship something that passes, then optimize the geomean' move — the checker validates reconstruction residual not elementwise match, so any numerically stable path qualifies. Value is as a safety net and as the measurement harness: every custom kernel is validated diff'd against this. Modest ceiling on its own (library launch overhead loses the tiny-batch entries) but de-risks the whole effort and matches the 'execution/ship' ethos.

**Receipts:** [https://docs.nvidia.com/cuda/cusolverdx/get_started/potrf.html] ("avoid per-batch library launch overhead—ideal for the thousands-of-tiny-matrices"); [doc_0800256bddaa9fe09044228b3ba496b3] ("researchers often cargo cult performance without a solid understanding on the un")
